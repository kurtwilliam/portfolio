import React, { Fragment } from "react";
import Body from "../Body";
import Header from "../Header";

const Home = () => (
  <Fragment>
    <Header />
    <Body />
  </Fragment>
);

export default Home;
